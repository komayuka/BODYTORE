<% if trainer.favorited_by?(current_customer) %>
    <%= link_to favorites_path(trainer), method: :delete, remote: true do %>
     ♥<%= trainer.favorites.count %> いいね
    <% end %>

    <% else %>

    <%= link_to favorites_path(trainer), method: :post, remote: true do %>
     ♡<%= trainer.favorites.count %> いいね
    <% end %>

<% end %>

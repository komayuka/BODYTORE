<div class="container">
  <div class="row">
    <div class="col">
      <h3>コメント一覧</h3>
      <table class="table">
        <thead>
          <tr>
            <th>名前</th>
            <th>タイトル</th>
            <th>内容</th>
          </tr>
        </thead>
        <tbody>
          <tr>
             <% @reviews.each do |review| %>
             <td><%= review.customer.customer_name %></td>
             <td><%= review.title %></td>
             <td> <%= review.body %></td>

          </tr>
          <% end %>
        </tbody>
      </table>

    </div>
  </div>
</div>
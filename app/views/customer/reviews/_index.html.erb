<div class="container">
  <div class="row">
    <div class="col">
      <h3>新着クチコミ一覧</h3>
      <%= render 'layouts/flash' %>


      <% reviews.each do |review| %>
       <%= review.customer_name %>
       <%= review.title %>
       <%= review.body %>
       <% end %>
    </div>
  </div>
</div>